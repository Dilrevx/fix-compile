# Environment variables for Dockerfile fixer
OPENAI_API_KEY=your-api-key-here

# Optional: specify a different model
# DOCKERFILE_FIXER_MODEL=gpt-4

# Optional: set logging level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO
